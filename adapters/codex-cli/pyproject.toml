# SPDX-FileCopyrightText: Copyright (c) 2026, NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

[build-system]
requires = [
  "setuptools>=77",
]
build-backend = "setuptools.build_meta"

[project]
name = "nemo-fabric-adapters-codex-cli"
version = "0.2.0"
description = "Codex CLI adapter for NeMo Fabric"
authors = [
  { name = "NVIDIA Corporation" },
]
keywords = ["agents", "ai", "nemo-fabric", "runtime", "tools"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
]
license = "Apache-2.0"
license-files = ["LICENSE"]
readme = "pypi.md"
requires-python = ">=3.11"
dependencies = [
  "nemo-fabric-adapter-contract == 0.2.0",
  "nemo-fabric-adapters-common == 0.2.0",
  "tomli-w~=1.2",
]

[project.optional-dependencies]
# The harness is the external `codex` executable; there is no Python target
# package to install. The extras stay published so the root delegation pattern
# is uniform across adapters.
harness = []
full = []

[project.urls]
Repository = "https://github.com/NVIDIA/NeMo-Fabric"
Homepage = "https://github.com/NVIDIA/NeMo-Fabric"
Issues = "https://github.com/NVIDIA/NeMo-Fabric/issues"
Documentation = "https://docs.nvidia.com/nemo/fabric"

[tool.setuptools.packages.find]
where = ["src"]
include = ["nemo_fabric_adapters.codex_cli*"]

[tool.setuptools.data-files]
"share/nemo-fabric/adapters/codex-cli" = ["fabric-adapter.json"]

[tool.uv.sources]
nemo-fabric-adapter-contract = { path = "../../adapter-contract", editable = true }
nemo-fabric-adapters-common = { path = "../common", editable = true }
